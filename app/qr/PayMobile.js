"use client";import {useState} from "react";export default function PayMobile(){const [busy,setBusy]=useState(false);return <button className="btn" disabled={busy} onClick={()=>{setBusy(true);location.href="/api/payment/init?usecase=qrcode"}}>{busy?"Ouverture…":"Payer 0,30 €"}</button>}