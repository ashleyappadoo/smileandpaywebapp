"use client";import {useState} from "react";
export default function PayButton({usecase,label="Payer"}){const [busy,setBusy]=useState(false);function go(){setBusy(true);location.href="/api/payment/init?usecase="+encodeURIComponent(usecase)}return <button className="btn" disabled={busy} onClick={go}>{busy?"Préparation…":label}</button>}