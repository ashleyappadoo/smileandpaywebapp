const labels={success:["Paiement accepté","Le paiement a bien été validé."],refused:["Paiement refusé","Le paiement n'a pas été accepté."],cancel:["Paiement annulé","Le parcours de paiement a été annulé."],error:["Erreur de paiement","Le paiement n'a pas pu être finalisé."]};
export default async function Page({params,searchParams}){const {status}=await params;const q=await searchParams;const [title,text]=labels[status]||labels.error;return <main className="wrap"><div className="brand"><div className="logo">S</div>Smile & Pay</div><section className="result"><span className="tag">{status.toUpperCase()}</span><h1>{title}</h1><p>{text}</p>{q.reason==="configuration"&&<div className="code">Démo prête. Configurez SNP_EPAY_ENDPOINT dans Vercel pour activer le paiement réel.</div>}<p><a className="btn" href="/">Retour à la démo</a></p></section></main>}